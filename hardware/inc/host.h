#ifndef _HOST_H_
#define _HOST_H_

#include "config.h"

// #define PROFILE
// #define DEV_INFO

typedef short proto_t; // same as input and weight type
typedef short lut_t;
typedef int dist_t;  // require dist_t to have larger size than proto_t
typedef char ind_t;
typedef short acc_t; // same as out type

#endif